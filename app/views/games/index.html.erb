<%= button_to "Create a game", games_path, method: :post, data: { turbo_method: :post } %>

<ul>
<%= @games.each do |game| %>
  <li><%= link_to "Join game #{game.id}", join_game_path(game), method: :put, data: { turbo_method: :put } %></li>
<% end %>
</ul>
